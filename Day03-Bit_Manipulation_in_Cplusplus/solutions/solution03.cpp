/*
 * Solution 3: Clear Bit (Bit Manipulation in Cplusplus)
 */
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin>>n; vector<int> a(n); for(int&x:a)cin>>x;
    /* XOR all, find partition bit, XOR each partition */
    return 0;
}
