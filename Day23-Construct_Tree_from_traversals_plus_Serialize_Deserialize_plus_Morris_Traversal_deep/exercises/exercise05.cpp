/*
 * Exercise 5: Recover BST Morris
 */
#include <bits/stdc++.h>
using namespace std;
struct TreeNode { int val; TreeNode *left,*right; TreeNode(int v):val(v),left(nullptr),right(nullptr){} };
// Exercise 5: Recover BST (two nodes are swapped)
// Hint: During inorder traversal find where order breaks (first and second candidate). Swap their values.
void recoverTree(TreeNode* root) { /* TODO */ }
int main() {}
